#### 概念理解

用户界面：客户能看到的，比如客户端

视图层：只针对前端界面

前端的主要工作：MVC中的V，制作前端页面效果

#### 发展历程

jq：解决了兼容性

模板引擎：解决了前端渲染

Vue框架：减少不必要的DOM操作；提高渲染效率；双向数据绑定；

#### 框架和库

框架：一套完整的解决方案；对项目的侵入性较大，不易更改

库（插件）：提供某个小功能，对项目的侵入性较小，容易切换

#### MVC和MVVM

MVC：后端分层概念

MVVM：前端视图层的概念，主要关注于MVC中的V；主要把每个页面分成了M、V和VM，VM是核心，因为它是M和V之间的调度者；

MVVM作用：提供数据的双向绑定，VM提供的

#### {{}}的意义

这两个括号意义不同，可以简单的理解为：

- 外面的花括号是说：我这里面写的是js语句
- 内部的花括号是说：我里面的不是要显示的文字，而是变量，我要把变量的值解析放在这

<!DOCTYPE html>

<html lang="en">

<head>

    <meta charset="UTF-8">

    <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>Document</title>

  <!-- 1导入Vue包 -->

    <script src="./lib/vue-2.4.0.js"></script>

</head>

<body>



    <div id="app">

        <p>

​      {{msg}}

​    </p>

  </div>

  

    <script>

​    var vm=new Vue({

​      el:'#app',

​      data:{

​        msg:''

​      }

​    });

  </script>

</body>

</html>





### webpack:

终端：进入项目文件夹

安装webpack：

npm i webpack -g

npm init -y

npm i jquery -S

webpack处理： 

webpack  要处理的文件  处理后的文件

例：webpack ./src/main.js ./dist/bundle.js

功能：1.



错误1：

![image-20210116211419226](D:\web\Vue\笔记.assets\image-20210116211419226.png)

原因：webapck-cli 4.x和webpack 5.x 不兼容

解决：卸载webapck-cli 4.x --> npm uninstall webpack-cli，然后安装webapck-cli 3.x  --> npm install webpack-cli@3 -D



错误2：

![image-20210116212031599](D:\web\Vue\笔记.assets\image-20210116212031599.png)

问题描述：运行成功，但界面还是原来的样子

原因：使用webpack-dev-server 打包输出的文件存在根目录下（看命令行第5行），而在HTML文件中的路径是dist文件夹

解决：修改HTML文件中的引用文件路径为： /bundle.js

扩展：这样生成的bundle.js在根路径中看不到，因为它是存在于内存中

错误3：

![image-20210116212541102](D:\web\Vue\笔记.assets\image-20210116212541102.png)

未解决:sob:，只是换成配置文件的形式打包了



  "dev": "webpack-dev-server --open --port 3000 --contentBase src --hot "

--open：运行时，自动启动浏览器界面

--port 3000：配置端口号

--contentBase src：配置初始展示路径

--hot ：热加载，在修改css样式时，无需刷新整个页面



#### 在内存中自动生成HTML页面：

1.命令行安装插件 

 npm i html-webpack-plugin -D

2.webpack.config.js中配置：

const htmlWebpackPlugin=require('html-webpack-plugin')

 plugins:[//配置插件的节点

  new htmlWebpackPlugin({

   // 创建一个在内存中生成HTML页面的插件

   // 根据指定的模板页面生成内存中的页面

   template:path.join(__dirname,'./src/index.html'),

   filename:'index.html'//指定生成的页面的名称，这个随意

  })

 ]

// 这个插件的两个作用：

// 1.自动在内存中根据指定页面生成一个内存的页面

// 2.自动把打包好的bundle.js 追加页面中去



#### 打包非JS文件

注：webpack，默认只能打包处理js类型的文件；如果要处理非js类型的文件，需要手动安装一下合适的第三方loader加载器

0.导入css文件，在main.js 中：import './css/index.css'

1.打包css文件，需要安装：npm i style-loader css-loader -D

2.打开webpack.config.js文件，在里面新增一个配置节点：module，是一个对象；

在这个对象上，有一个rules属性，是个数组，其中存放了所有第三方的匹配和处理规则



less文件：

导入：import './css/index.less'

命令行：npm i less -D 

 配置代码： {test:/\.less$/,use:['style-loader','css-loader','less-loader']},



.scss文件：

导入：import './css/index.scss'

命令行：npm i sass-loader -D      ;    npm  i node-sass -D

 配置代码：{test:/\.scss$/,use:['style-loader','css-loader','sass-loader']}



图片：

命令行： npm i url-loader file-loader -D

配置代码：

   // 图片,一个loader时，可以直接用字符串

   {test:/\.(jpg|png|gif|bmp|jpeg)$/,use:'url-loader'}



Bootstrap字体图标

命令行：安装     npm i bootstrap-icons

导入：import 'bootstrap-icons/font/bootstrap-icons.css'

配置代码：

   {test:/\.(woff|ttf|woff2|eot|svg)$/,use:'url-loader'}



ES6部分和更高级的语法

命令行：

 npm i babel-loader @babel/core @babel/preset-env @babel/plugin-transform-runtime -D 

 npm i @babel/runtime -D

 npm i @babel/plugin-proposal-class-properties -D

配置代码：

{test:/\.js$/,use:'babel-loader',exclude:/node_modules/}

或者：{test:/\.js$/,use:{loader:'babel-loader'}, exclude: /(node_modules|bower_components)/}

.babelrc文件代码：

  // preset可以当做“语法”

  {

  "presets":["@babel/preset-env"],

   // 对应之前装的插件，不需要带前缀

  "plugins": ["@babel/plugin-transform-runtime","@babel/plugin-proposal-class-properties"]

}



VScode中.vue文件不出现报错的办法：

在首选项设置中：files：associations，添加*.vue   html，即把vue当成HTML文件看待

## 指令：

### v-on

绑定事件

v-on:click='add'    //  @事件=方法名

 @click='reduce'

### v-model

绑定数据源

 v-model="message"

v-model.lazy="message"        //失去焦点时（我按回车也是），同步数据

v-model.number="message"  //必须是数字，但必须先输入数字，否则就没用了

v-model.trim="message"      //将用户输入的前后的空格去掉,中间的去不掉

绑定：文本框，文本域，多选框（数组，单个值），单选框

### v-bind

绑定标签属性

内容比较多，其实就是绑定对象，数组，

v-bind:class='className'

v-bind:class='[className，。。。]'

v-bind:class='{className: isOK}'

v-bind:class='{color:colorName,。。。}'

v-bind:class='styleName'

### v-for

循环：数组

v-for="item in sortItems"   //  值  in 数组名

v-for='(student,index) in sortStudents'     // (值，索引) in 数组名

### v-if  v-else-if v-else

判断

v-if='isLogin'

### v-text  & v-html

v-text='message'

在加载时，先出现{{message}}  v-text可以避免

v-html='dodo'

 识别html标签，尽量少用，会被黑客攻击

### v-pre &v-cloak &v-once

​    <!-- 原样输出 -->

<div v-pre>{{message}}  <div>  

   <div v-cloak>渲染完成后才显示</div>

​    <!-- 只进行一次渲染 ，后面变换就不跟随了-->

        <div v-once>{{message}}</div>

# VueCli

安装：

```bash
npm install -g @vue/cli
# OR
yarn global add @vue/cli
```

创建一个项目

vue create projectName

查看版本

vue -V



启动项目

yarn  serve



# vue-router

1、建.vue文件，里面写模板（结构），js（暴露组件）,样式

2、在router中的index文件中，导入组件，建立路由

3、在APP.vue中，加入路由结构